# This is a deliberately invalid container config to test with:
# This test should fail as although it's a valid yaml file the 
# file has the wrong extension.
Example_Model1:
  description: "this is a .txt not a .yaml file"
  container_definition: "alpine:latest"