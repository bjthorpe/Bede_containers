# This is a deliberately invalid container config to test with:
# Test 6 checks for an edge case as we should get an error 
# if a file contains definitions for more than one container 
# with the same name.
Example_Model1:
# This spec is valid
  description: "another example to test config files" 
  image_file: null
  repo_url: "docker://alpine:latest"
  shared_directories: null
#--------------------------------------
# This is also valid
Example_Model1:
  description: "another example to test config files"   
  image_file: 'test.sif'