# This is a deliberately invalid container config to test with:
# Test 7 checks that a model name that contains anything but 
# letter, numbers, hyphens and underscores is caught as invalid. 
Example_Model1:
# This spec is valid
  description: "another example to test config files" 
  container_definition: "alpine:latest"
#--------------------------------------
# This is not valid as the name contains a space
Example Model1:
  description: "another example to test config files"   
  image_file: 'test.sif'
